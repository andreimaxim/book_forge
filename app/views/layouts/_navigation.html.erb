<nav class="bg-white border-b border-gray-200 fixed top-0 left-0 right-0 z-50" data-controller="mobile-menu">
  <div class="container mx-auto px-5">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center space-x-8">
        <a href="<%= root_path %>" class="text-xl font-bold text-gray-900">
          BookForge
        </a>
        <div class="hidden md:flex items-center space-x-1" data-testid="desktop-nav">
          <%= link_to "Dashboard", root_path, class: nav_link_class(root_path) %>
          <%= link_to "Authors", authors_path, class: nav_link_class(authors_path) %>
          <%= link_to "Publishers", publishers_path, class: nav_link_class(publishers_path) %>
          <%= link_to "Agents", agents_path, class: nav_link_class(agents_path) %>
          <%= link_to "Prospects", prospects_path, class: nav_link_class(prospects_path) %>
          <%= link_to "Books", books_path, class: nav_link_class(books_path) %>
          <%= link_to "Deals", deals_path, class: nav_link_class(deals_path) %>
        </div>
      </div>

      <!-- Search bar -->
      <div class="hidden md:flex items-center">
        <%= render "search/search_bar" %>
      </div>

      <!-- Mobile menu button -->
      <button
        type="button"
        class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-gray-600 hover:text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-blue-500"
        data-testid="mobile-menu-button"
        data-action="click->mobile-menu#toggle"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <span class="sr-only">Open main menu</span>
        <!-- Hamburger icon -->
        <svg class="h-6 w-6" data-mobile-menu-target="openIcon" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
        <!-- Close icon (hidden by default) -->
        <svg class="h-6 w-6 hidden" data-mobile-menu-target="closeIcon" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu panel -->
  <div
    class="md:hidden hidden"
    data-testid="mobile-menu"
    data-mobile-menu-target="menu"
  >
    <div class="px-2 pt-2 pb-3 space-y-1 bg-white border-b border-gray-200">
      <%= link_to "Dashboard", root_path, class: mobile_nav_link_class(root_path) %>
      <%= link_to "Authors", authors_path, class: mobile_nav_link_class(authors_path) %>
      <%= link_to "Publishers", publishers_path, class: mobile_nav_link_class(publishers_path) %>
      <%= link_to "Agents", agents_path, class: mobile_nav_link_class(agents_path) %>
      <%= link_to "Prospects", prospects_path, class: mobile_nav_link_class(prospects_path) %>
      <%= link_to "Books", books_path, class: mobile_nav_link_class(books_path) %>
      <%= link_to "Deals", deals_path, class: mobile_nav_link_class(deals_path) %>
    </div>
  </div>
</nav>
